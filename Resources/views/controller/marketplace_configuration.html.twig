{% extends '@PrestaShop/Admin/layout.html.twig' %}
{% set layoutTitle = 'Marketplace configruation' %}

{% block content %}
  <div class="row justify-content-center">
    <div class="col">
      {{ form_start(form) }}

      <div class="card">

        <div class="card-header">
          {{ 'Main configuration' }}
        </div>

        <div class="card-body">
          {#                    <div class="form-group row">#}
          {#                        <div class="form-control-label">{{ 'Category ID for shipping' }}</div>#}
          {#                        <div class="col-sm-4">#}
          {#                            {{ form_errors(form.BWMARKETPLACE_CATEGORY_ID) }}#}
          {#                            {{ form_widget(form.BWMARKETPLACE_CATEGORY_ID) }}#}
          {#                        </div>#}
          {#                    </div>#}
          {#                    <div class="form-group row">#}
          {#                        <div class="form-control-label">{{ 'Taxrule ID for shipping' }}</div>#}
          {#                        <div class="col-sm-4">#}
          {#                            {{ form_errors(form.BWMARKETPLACE_TAXRULE_SHIPPING) }}#}
          {#                            {{ form_widget(form.BWMARKETPLACE_TAXRULE_SHIPPING) }}#}
          {#                        </div>#}
          {#                    </div>#}
          {#                    <div class="form-group row">#}
          {#                        <div class="form-control-label">{{ 'Prefix shipping name' }}</div>#}
          {#                        <div class="col-sm-4">#}
          {#                            {{ form_errors(form.BWMARKETPLACE_PREFIX_SHIPPING_NAME) }}#}
          {#                            {{ form_widget(form.BWMARKETPLACE_PREFIX_SHIPPING_NAME) }}#}
          {#                        </div>#}
          {#                    </div>#}
          <div class="d-flex justify-content-center">
{#            <a class="btn btn-primary" href="{{ path('admin_marketplace_install_demo') }}">{{ 'Install demo'|trans({},'Admin.Marketplace.Configuration') }}</a>#}
          </div>
        </div>
        <div class="card-footer">
          <div class="d-flex justify-content-end">
            <button class="btn btn-primary">{{ 'Save'|trans({},'Admin.Action') }}</button>
          </div>
        </div>
      </div>
      <div style="display: none">
        {{ form_rest(form) }}
      </div>
      {{ form_end(form) }}

    </div>
  </div>
{% endblock %}
